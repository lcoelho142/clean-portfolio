import Layout from "../Layout";
import AboutContent from "../AboutContent";

export default function AboutPage() {
    return (
        <Layout>
            <AboutContent />
        </Layout>
    );
}